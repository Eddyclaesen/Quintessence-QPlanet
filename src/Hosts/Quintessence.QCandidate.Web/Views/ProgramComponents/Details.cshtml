@model Quintessence.QCandidate.Models.ProgramComponentModel

<div class="context">
    <h1>@Model.Title</h1>
</div>

@if (Model.CanShowPdf)
{
    <iframe src="@Model.PdfUrl" width="100%" height="1075px">
    </iframe>
}