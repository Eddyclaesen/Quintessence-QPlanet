@model Quintessence.QPlanet.ViewModel.Dim.DictionaryLevelModel
<li><span datamodel-id="@Model.Id">@Html.DisplayTextFor(m => m.Name)</span>
    <ul>
        @for (int i = 0; i < Model.DictionaryIndicators.Count; i++)
        {
            @Html.DisplayFor(m => m.DictionaryIndicators[i])
        }
    </ul>
</li>
