@model Quintessence.QPlanet.ViewModel.Dim.DictionaryClusterModel
<li><span datamodel-id="@Model.Id">@Html.DisplayTextFor(m => m.Name)</span>
    <ul>
        @for (int i = 0; i < Model.DictionaryCompetences.Count; i++)
        {
            @Html.DisplayFor(m => m.DictionaryCompetences[i])
        }
    </ul>
</li>
