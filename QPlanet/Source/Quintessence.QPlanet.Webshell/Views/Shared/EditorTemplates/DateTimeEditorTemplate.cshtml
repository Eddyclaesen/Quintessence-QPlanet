@model DateTime
@Html.TextBoxFor(m => m, new { @Value = Model.ToString("dd/MM/yyyy HH:mm") })
<script>
    $(function () {
        $('#@Html.IdFor(m => m)').datetimepicker({ dateFormat: "dd/mm/yy", timeFormat: "hh:mm" });
    })
</script>
